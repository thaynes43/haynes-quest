# Authoring activation evidence (2026-09-11)

Executed from outside the pod per the [activation runbook](../../.agents/runbooks/activate-dev-env.md), with Tom's explicit authorization for the held-PR merge.

- **Preflight.** #2833's diff touched only `kubernetes/main/apps/dev/dev-env/app/helmrelease.yaml`: it removed the temporary `configmaps.exclude.reloader.stakater.com/reload` Deployment annotation and added the pod-template annotation `haynesops.com/config-generation: quest-authoring-2026-09-11`; no image bump. All nine `flux-local` checks passed. The five staged files (`CLAUDE.md`, `mcp.json`, `config.toml`, `AGENTS.header.md`, `requirements.toml`) were byte-identical across Git `main`, the live `dev-env-config-claude`/`dev-env-config-codex` ConfigMaps and the pod mounts (including `/etc/codex/requirements.toml`). Before the merge dev-env was pod `dev-env-7778f77687-9vsbw`, UID `5cedae51-83ee-4e29-b3ef-ec96886bf938`, Deployment revision 50, zero restarts, image `0.5.4@sha256:80a2c0b8…`. Maintenance was declared on the pod (`act-033026-84136`, scope `dev,dev-env,talosw02`) and ended after verification.
- **Merge and rollout.** #2833 squash-merged at 2026-09-11T03:30:43Z as `2cbe79bef2486da4300e9167b04c89a345e096c3`. `flux reconcile kustomization dev-env -n dev --with-source` applied that revision (Helm release `dev/dev-env.v31`); the Deployment moved to revision 51 with exactly one ReplicaSet swap (`7778f77687` → `dfdd8c894`, strategy Recreate). No manual restart or pod deletion was added, and no second rollout followed.
- **New pod.** `dev-env-dfdd8c894-l724p`, UID `c3a94756-af35-405e-93bc-eb05c2979d3a`, on `talosw02`, created 2026-09-11T03:31:44Z, Ready 3/3 (`app`, `auth-watch`, `gh-refresher` all zero restarts; `k8tz` init exited 0). Same image digest `0.5.4@sha256:80a2c0b8…`. The pod carries `haynesops.com/config-generation=quest-authoring-2026-09-11`; the Deployment keeps `reloader.stakater.com/auto: "true"` and the exclusion annotation is gone, so normal Reloader behaviour is restored. The `verify-thaynes43-images` Kyverno PolicyViolation warnings on the Deployment are the pre-existing audit-mode signature gap for this image (the previous pod carried the same `fail` annotation), not a result of this change.
- **Startup.** dev-init registered nine MCP servers including `audio` and `blender`, rendered `~/.codex/config.toml` with nine `[mcp_servers.*]` entries (both authoring URLs present) and `~/.codex/AGENTS.md` with the native `gpt-5.6-sol`/`xhigh` Codex subagent rule and the `claude-opus-5`/`xhigh` Claude Code subagent rule. post-ready settled at 03:33:17Z: codex remote-control up, and the Claude standby session `haynes-ops-0910-233312` up on `claude-fable-5-1` at `xhigh`.
- **Fresh-session discovery and calls.** `claude mcp list` in the new pod reported `audio` and `blender` as HTTP servers, both ✔ Connected. `codex mcp list` showed both enabled at their cluster URLs. Raw MCP `initialize` + `tools/list` from inside the pod returned "Haynes Quest Blender 1.29.0" with `get_scene_info`, `get_object_info`, `execute_blender_code`, `get_viewport_screenshot`, and "Haynes Quest Audio 1.29.0" with `generate_sound`, `get_generation`, `cancel_generation`, `list_generations`. A fresh `codex exec -m gpt-5.6-sol` session then called `blender/get_scene_info` (one mesh `QuestServiceFixture`, two materials) and `audio/list_generations` (five prior jobs) natively. Note: `gpt-5.4-mini` is rejected on the ChatGPT account ("not supported when using Codex with a ChatGPT account"); use the TEAM.md models.
- **Surroundings.** `blender-authoring` and `audio-authoring` HelmReleases stayed Ready with their pods 1/1 and unchanged; the `haynes-quest`, `haynes-quest-immich` and `haynes-quest-db-init` ExternalSecrets still report `SecretSynced`. The restart ended the agent sessions that were running in the old pod (expected and authorized); their worktrees remain on the PVC as stranded entries for `agent-run prune`. No game implementation, OAuth setup, 1Password change or additional restart was performed.


This is the completed pre-MVP activation record. The overnight implementation did not authorize another dev-env restart.
