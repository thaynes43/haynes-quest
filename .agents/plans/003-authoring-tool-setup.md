# PLAN-003: Set up authoring dependencies before development

- **Status:** Complete (2026-09-11; single dev-env activation merged and fresh-session discovery verified — see [HANDOFF.md](../HANDOFF.md) for the full evidence)
- **Owner direction:** Tom, 2026-09-10: prepare missing dependencies such as Blender first; use development agents with empty context windows
- **Related:** [DESIGN-002](../../docs/designs/002-asset-pipeline.md), [DESIGN-007](../../docs/designs/007-poc-development-loop.md), [DESIGN-008](../../docs/designs/008-audio-pipeline.md), [PLAN-002](002-foundation-prototype.md)

## Scope and sequence

Tom requires independent Blender/audio workloads and **one dev-env restart only after the needed dependencies and startup rules are ready**. Stage the initial registrations and startup Markdown while their ConfigMaps are temporarily excluded from automatic reloads. Keep the single explicit pod-template activation in held [haynes-ops PR #2833](https://github.com/thaynes43/haynes-ops/pull/2833). Later authoring service upgrades follow normal checked GitOps PRs without rolling dev-env.

1. Deploy and verify the independent Blender service, with its own persistent workspace, private HTTP MCP endpoint, confined artifact transfer, software graphics, and pinned export/validation tools. This is complete; evidence is below.
2. Deploy the independent audio service using official optimized Stable Audio Small-SFX CPU inference. Pin source/weights, provision the anonymous model downloads through a separate job, mount models read-only at runtime, and keep output/job metadata on a separate PVC. Expose a private asynchronous MCP interface and controlled downloads. No GPU or hosted subscription is needed for this baseline.
3. Run a real synthetic generation, inspect duration/format/checksum, record generation time and peak memory, and preserve the output for audition. Check asynchronous status/cancellation, artifact confinement, persistence across an audio-pod replacement, and dev-env continuity. Separate infrastructure evidence from final sound quality and Tom's exact-version asset approval.
4. Stage both verified service endpoints and startup rules while the reload exclusion is live: Codex native subagents use `gpt-5.6-sol` at `xhigh` with `fork_turns: "none"`; Claude Code subagents use `claude-opus-5` at `xhigh`; Astra leads Codex work. Verify the staged ConfigMaps and mounted files match Git while the pod stays unchanged. Then finalize #2833 to remove the temporary exclusion and add one pod-template activation marker. Review the complete diff and required checks before Tom's natural-break merge. That is the one planned dev-env restart.
5. In the fresh session, verify both generated MCP registrations and live calls. Then execute PLAN-004 under [TEAM.md](../TEAM.md), including its Astra-only Blender and authorized Fable exceptions, with fresh agents and self-contained work orders, required readings, owned paths, stable contracts, and remote artifact conventions. Final asset promotion still needs Tom's review.

GPU render/audio jobs, Small-Music, and voice generation remain later options. Existing GPU consumers omit some resource requests, so measure actual free VRAM and workload needs before selecting hardware. Dual GPUs do not imply pooled model memory.

## Current evidence

- Planning/docs PR #14 is merged at `e19c6224c35c1f94c58b5ad035d2f1999ad3c5bd`.
- Before this setup, the live pod has Xvfb/Mesa, Node, pnpm, Python, and uv; Blender, FFmpeg/ffprobe, xauth, and glTF tooling are missing from PATH. No GPU is assigned to the pod. Main-container memory limit is 64Gi; rootfs is read-only, home PVC/tmp writable.
- [Image PR #2831](https://github.com/thaynes43/haynes-ops/pull/2831) merged at `ec05e47e13ef61f75a4fe7cd79611ac66364ea82`; [offline container smoke passed](https://github.com/thaynes43/haynes-ops/actions/runs/34544394268). It proved MCP, screenshot, save/reopen, GLB validation, restart, and audio encoding. [Main build/publication/signing passed](https://github.com/thaynes43/haynes-ops/actions/runs/34544898212), and the registry manifest hash was independently verified. The historical image is `0.6.0@sha256:22029f8ffb0652c1caca744db9e97c3e4d874a6bd5253a6555254b2b2d541ea1`. Its in-dev-env Blender activation is superseded by the dedicated-service ruling; this image will not be activated for Blender. These tests do not prove the new remote deployment.
- [PR #2833](https://github.com/thaynes43/haynes-ops/pull/2833) contained only the final single-restart activation: remove the temporary Reloader exclusion and add a pod-template marker. Both remote MCP registrations and native Sol/Opus startup rules were staged through merged #2844 and verified in live ConfigMaps and mounted files without restarting dev-env. It contained no dev-env image bump. [Cleanup PR #2834](https://github.com/thaynes43/haynes-ops/pull/2834) removed superseded Blender layers from future dev-env images. #2833 squash-merged on 2026-09-11 as `2cbe79bef2486da4300e9167b04c89a345e096c3`; the post-activation evidence is below.
- [Log-access fix #2832](https://github.com/thaynes43/haynes-ops/pull/2832) merged and reconciled; the exact GitHub Actions log hostname now works from the pod.
- Live inventory found one RTX 3090 (`talosw01`), an A2000 (`talosm01`), and an RTX 2000 Ada (`talosm03`), each with existing consumers. Some consumers omit GPU requests, so scheduler availability overstates spare capacity. Tom also offered a server capable of dual 3090s. No GPU has been reserved for authoring. The optimized CPU audio service has provisioned its pinned model bundle anonymously and passed real generation; evidence follows below. DESIGN-008 records its pins and published download route. No generated audio is approved.

- Dedicated [service image PR #2835](https://github.com/thaynes43/haynes-ops/pull/2835) is merged at `6f1cb2f9fa701c467f942ec791d125e0ad6d2580`. [Main container smoke, publication, and signing passed](https://github.com/thaynes43/haynes-ops/actions/runs/34547117946). Anonymous registry verification confirmed `ghcr.io/thaynes43/blender-authoring:0.1.0@sha256:c626b9ac4efa241172929e3646f30be58cec35d4e6d460979b02d1f6a870257d`. [Deployment PR #2836](https://github.com/thaynes43/haynes-ops/pull/2836) is merged at `fc80a9a274b5b075b040dde7d67193f5b89c7c9b` and pins that digest. Flux reports both `blender-authoring` and dev-env ready at this revision.

- Live service validation on 2026-09-11 passed: native HTTP MCP initialization and all four tools, synthetic mesh/material creation, a 640×373 viewport PNG, `.blend`/GLB download and reimport, Khronos validation with zero errors, a 48kHz test cue via FFmpeg/ffprobe, and matching SHA256 hashes for all six transferred artifacts. Inputs/outputs live under `/workspace/candidates/setup-20260911`; dev-env evidence is `/tmp/quest-blender-live/` (temporary). Synthetic fixtures are not approved game assets.
- Replacing only the Blender pod changed its UID, after which the saved scene reopened from the 10Gi CephFS PVC and artifact downloads still matched. Dev-env retained the same pod UID, image digest, and zero restarts throughout. `/healthz` and `/readyz` return HTTP 200. The service is callable now over HTTP; native agent tool discovery waits for the held registration. No GPU was assigned.

### Self-hosted audio evidence

- Source [haynes-ops #2840](https://github.com/thaynes43/haynes-ops/pull/2840), deployment [#2841](https://github.com/thaynes43/haynes-ops/pull/2841), and verified download-host policy [#2842](https://github.com/thaynes43/haynes-ops/pull/2842) are merged. A separate manual provisioning Job downloaded the three pinned optimized model files anonymously (2,491,906,768 bytes), then atomically published their SHA256/size manifest. Runtime mounts models read-only and has no external egress or GPU.
- Cache fix [#2845](https://github.com/thaynes43/haynes-ops/pull/2845) passed [main build, container smoke, publication, and signing](https://github.com/thaynes43/haynes-ops/actions/runs/34553801735). [Deployment #2846](https://github.com/thaynes43/haynes-ops/pull/2846) pins `ghcr.io/thaynes43/audio-authoring:0.1.1@sha256:370be35723f8a66a4dbfe5f4ce4b4b9e1ff73c74e42d90bb28e942b2712f57bb`; registry manifest bytes independently matched this digest. Flux applied `a76e2cfae9f1028d7759eb336342c7bdf6e63bd9`.
- Live MCP initialization and all four tools passed at `http://audio-authoring.dev.svc.cluster.local:8000/mcp`: asynchronous submission/status, active and queued cancellation, history, completed WAV download, and rejected artifact escapes. One generation runs at a time. The service owns separate 10Gi CephFS model and workspace PVCs, both prune-protected; artifacts use `/artifacts/<job-id>/output.wav`.
- Actual CPU inference on `talosw01`, using a synthetic magical-chime prompt, ten seconds, eight steps, and seed 43, completed in 7.329 seconds on 0.1.0 and 7.046 seconds on 0.1.1. Final peak subprocess RSS was 4,151,181,312 bytes (about 3.9 GiB); pod requests are 1 CPU/4Gi, limits 4 CPUs/16Gi. The stereo 44.1kHz PCM16 WAV is exactly ten seconds and 1,764,044 bytes. Both jobs produced SHA256 `b36dd6e28ed14bae0dc67445608e05a80d6f2398ad34cb78ce8f080680ac8096`, peak -17.28 dBFS, and zero full-scale samples. These samples have not been auditioned or approved for gameplay.
- Replacing only the audio pod during the upgrade preserved the old WAV checksum and completed/cancelled job history without rewriting provenance. New job `b9cc5590a151442ea1a16e18b8096643` records service version 0.1.1; its WAV and metadata persist under `/workspace/jobs/<job-id>/`. The decoder now writes its cache under `/tmp/audio-xnnpack`, with no read-only/cache-open errors; a second five-second cue successfully reused that cache. Temporary dev-env evidence is `/tmp/quest-audio-live/`; the service PVC retains the outputs across the pending dev-env restart.
- After these checks, dev-env still has its original pod UID `5cedae51-83ee-4e29-b3ef-ec96886bf938`, unchanged pod template and image, Deployment revision 50, and zero container restarts. Detailed operating instructions and evidence live in the [ops runbook](https://github.com/thaynes43/haynes-ops/blob/main/.agents/runbooks/audio-authoring.md). Both service checks pass; the single activation and fresh-session discovery are recorded below.

### Single activation and fresh-session evidence (2026-09-11)

- Executed from outside the pod under [the activation runbook](../runbooks/activate-dev-env.md). Preflight confirmed #2833's diff (HelmRelease only, no image bump), nine passing `flux-local` checks, and byte-identical staged files across Git, both live ConfigMaps and the pod mounts. Maintenance was declared on the pod (`act-033026-84136`) and ended after verification.
- `flux reconcile kustomization dev-env -n dev --with-source` applied `main@2cbe79be` (Helm release `dev/dev-env.v31`). One ReplicaSet swap (`7778f77687` → `dfdd8c894`), Deployment revision 50 → 51, no extra restart or pod deletion, no second rollout.
- New pod `dev-env-dfdd8c894-l724p` (UID `c3a94756-af35-405e-93bc-eb05c2979d3a`, `talosw02`) is Ready 3/3 with zero restarts on the same image digest, carries `haynesops.com/config-generation=quest-authoring-2026-09-11`, and the Deployment's exclusion annotation is gone while `reloader.stakater.com/auto` remains. Pre-existing Kyverno image-signature audit warnings are unchanged by this rollout.
- dev-init registered nine MCP servers including `audio` and `blender`, rendered `~/.codex/config.toml` with both authoring URLs and `~/.codex/AGENTS.md` with the native `gpt-5.6-sol`/`xhigh` and `claude-opus-5`/`xhigh` subagent rules; post-ready brought codex remote-control and the Claude standby session back.
- Fresh-session discovery: `claude mcp list` shows `audio` and `blender` ✔ Connected; `codex mcp list` shows both enabled; raw MCP `initialize`/`tools/list` from the pod returned four tools from each server (Blender 1.29.0, Audio 1.29.0); a fresh `codex exec -m gpt-5.6-sol` session called `blender/get_scene_info` and `audio/list_generations` natively. `gpt-5.4-mini` is not usable on the ChatGPT account.
- Both authoring services and the three `haynes-quest` ExternalSecrets stayed Ready throughout.

## Completion

Complete when authoring tools are live and their checks pass, the selected self-hosted audio route has passed its live trial, and the fresh-context development handoff is self-contained. A green image build alone does not prove the remote MCP connection, artifact transfer, or persistence. A held registration draft is not a connected dependency; GPU capacity alone does not complete an audio trial.

## Single-restart preflight

Reloader v1.4.22 handles changed ConfigMaps separately. Claude/MCP settings and the Codex startup header occupy two ConfigMaps, so one PR changing both does not itself guarantee one rollout. [Preparatory PR #2843](https://github.com/thaynes43/haynes-ops/pull/2843) temporarily excludes only those two maps on Deployment metadata. Live comparison confirmed unchanged pod template, Deployment revision 50, original pod UID, and zero restarts. Scripts/Secret reload behavior remains active.

[Configuration stage #2844](https://github.com/thaynes43/haynes-ops/pull/2844) merged both endpoint registrations and the native Sol/Opus rules. All three source files match the live ConfigMaps and mounted files. Dev-env retained Deployment revision 50, its original pod UID, and zero container restarts. The final activation restores normal reload behavior and makes one explicit pod-template change after configuration is already present. It contains no dev-env image bump. Fresh-session MCP discovery passed after the activation (evidence above).
