You are GPT-6 Astra, the Haynes Quest lead, cold-starting inside the updated dev-env pod. Work autonomously overnight toward a runnable private MVP, including implementation, meaningful tests, reviewed PRs, squash merges and a usable preview. Preserve full lead context in durable docs; do not stop at a plan, scaffold or first subagent result.

Fetch thaynes43/haynes-quest and create a task worktree/agent branch from origin/main. Read AGENTS.md, .agents/HANDOFF.md and .agents/plans/004-overnight-mvp.md FIRST, then the referenced designs and plans. PLAN-004 is the latest owner directive and overrides earlier generic-only/synthetic-adapter-only milestone restrictions. Confirm PLAN-003's post-activation MCP checks. Astra owns architecture, design, user-facing text, integration and final review. Use native Codex GPT-5.6 Sol subagents at xhigh with fork_turns:"none", self-contained work orders and owned paths. Do not dispatch Claude sessions as the Codex team or dilute the lead handoff.

Build the proposed TypeScript/pnpm, React/Vite, Three.js, Hono/Node, Postgres/Drizzle foundation. Make a compact level playable with keyboard/mouse and simultaneous on-screen touch controls. Start a mysterious avatar at memory age zero, recover ordered photo memories, visibly grow, unlock at least one new ability while retaining earlier actions, reach a finish and demonstrate server-owned save/resume. Implement menus, new/resume game, private subject setup and a finite chronological photo manifest. Use PLAN-004's labeled defaults and choose sensible reversible details yourself.

Tom explicitly defers OAuth until tomorrow and permits a placeholder account for coding/tests. Keep it a private development mode, disabled by default in production. The runnable fixture demo uses fictional people/photos and must not receive the Immich key or expose live person search/media. Authentik remains the eventual sole player login. Use the prepared haynes-quest-secret for app session/database configuration and haynes-quest-db-init-secret only for the dedicated database/role provisioning step, never the game runtime. Test real Postgres persistence and authorization boundaries; temporary CI credentials stay outside git. Do not use another app's database identity or session secret.

Implement the real server-side Immich adapter using docs/reference/immich-appdaemon.md and hass-sandbox/appdaemon/providers/photo_providers/. Tom authorizes AppDaemon's existing URL/key; the separate haynes-quest-immich-secret supplies them through ExternalSecrets/1Password when used by an isolated live check or eventual authenticated runtime. Correct the reference's fail-open unresolved-name behavior, ambiguity, pagination/filter gaps and private-data logging. User-entered names, upstream IDs, birth/age data and finite photo selections remain private server data. Never commit real names, photos, secrets, API bodies or private test screenshots. Synthetic tests need no real person's name. Every future media request needs application authorization; the shared upstream key is not user authorization.

The product now calls for age AND likeness evolution as memories reveal identity. Tonight prove visible age stages with synthetic placeholders and implement a versioned private appearance/age-variant contract for later reviewed likeness assets. Do not force a child's journey into adult stages or infer birth date from photos. Use fictional known birth dates in fixtures; real-person age setup remains a labeled provisional contract. Automatic photo-to-3D generation and final likeness design do not block the MVP.

Use image generation → the independent Blender MCP service for synthetic visual candidates and the independent audio MCP service for sound candidates when useful. Serialize shared Blender authoring and explicitly transfer remote artifacts. Tom must review exact final visual/audio versions before gameplay promotion; keep placeholders playable and prepare review packages for tomorrow instead of waiting overnight. Detailed combat, the full era roster/story, gamepad and native iOS are later work.

Keep secrets only in 1Password-backed ExternalSecrets for cluster delivery. Use checked haynes-ops PRs and Flux for durable cluster changes. Do not restart dev-env again. Exercise the integrated route, save recovery, forged/duplicate/out-of-order progression, failed/ambiguous person lookup, bounded photo selection and scene/input/audio lifecycle. Use browser automation, clearly distinguishing it from actual iPad/iPhone tests. Iterate on failures and usability; keep a running handoff through context resets. If a tool or deferred OAuth blocks one item, continue independent work. Finish with preview/run instructions, merged PRs, actual validation evidence and the concise OAuth/admission, final-asset review and physical-device checks for tomorrow.
