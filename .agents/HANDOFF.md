# Current handoff

- **Updated:** 2026-09-11, 04:56 UTC
- **Stage:** Playable MVP merged; private image/rollout and remaining first-pass prop catalog in progress
- **Lead/team:** driving GPT-6 Astra, max, with full project context. [TEAM.md](TEAM.md): fresh native Sol xhigh for ordinary work, fresh Astra max for all Blender work, authorized separate Fable 5.1 xhigh review/coding. Lead alone generates images serially and owns UI/copy/art decisions.
- **Current task:** [PLAN-004](plans/004-overnight-mvp.md), authorized through private playable MVP, scoped first-pass candidates, checked PR merges and live verification. OAuth tomorrow; **no dev-env restart**. Pending Tom asset approval does not block candidate production or ordinary merges.

## Resume here

Root worktree `/home/dev/work/haynes-quest-overnight-mvp`, now branch `agent/quest-catalog` based on main `6263dc4`. Main MVP [PR #21](https://github.com/thaynes43/haynes-quest/pull/21) squash-merged at 04:55:40UTC as `6263dc42443eb5c33943d26f668da386df4900d4`. The previous implementation branch was `agent/haynes-quest-overnight-mvp`, final 639e0a9. Read [DESIGN-009](../docs/designs/009-overnight-contracts.md), [WO-004](work-orders/004-coordinator.md), and the [private-preview runbook](../docs/ops/002-private-preview.md).

1. **Deploy:** fresh Sol `deploy_preview`, worktree `/home/dev/work/quest-private-deploy`, branch `agent/quest-private-deploy`, carries the reviewed haynes-ops preparation `c4b88c2ec8526859f62dae944ac49ec168c31405` forward. Main's app image is publishing; wait for the exact merge-commit tag/digest and anonymous-pull verification. Then concrete ops PR, root review, green checks, squash merge, Flux and actual pod-replacement persistence proof. Do not deploy the intentional image placeholder. Its source work order is `.agents/work-orders/2026-09-11-haynes-quest-mvp-ops.md` in haynes-ops. Private hostname reserved: `https://haynes-quest.haynesops.com`; not yet claimed live at this checkpoint.
2. **Author:** fresh Astra max `blender_clearing` exclusively owns [WO-010](work-orders/010-blender-clearing-kit.md), worktree `/home/dev/work/quest-blender-clearing`, base 2308cb0. Seven static pieces across the five remaining catalog IDs; all exact concepts are committed. Remote `/workspace/haynes-quest/clearing-kit/v001`. No competing scene writes until explicit release. Three earlier dispatch attempts hit a temporary thread limit; retry succeeded after browser verification completed. No downgrade or CLI substitution occurred.
3. **Intake/publish:** lead compares actual prop exports with their concepts, checks GLB/browser views and updates all five review pages with media, checksums and actual findings. Complete catalog PR through checks/merge, then let the deployment lane update the immutable image and verify the complete private studio. Only afterward record PLAN-004 completion and archive it with links fixed.

## What is implemented and verified

The React/Vite/Three.js client provides home, finite fictional setup/selection, a compact playable clearing, touch and keyboard/mouse controls, chronological recovery, infant-to-child growth, jump unlock, reachable finish, album, save/resume and mute/volume preferences. A short action buffer prevents quick taps being lost between frames; leaving the raised step resumes gravity. Flat routes cover selections without jump. Gameplay uses temporary code-built shapes and synthetic pictures; the approved sound map is empty/silent.

Hono/Node and Postgres/Drizzle own sessions, previews, manifests and progression. A signed HttpOnly cookie identifies a server-assigned fictional player; browser identity/age/unlock fields are rejected. Same-origin JSON mutations require the explicit CSRF header. Ownership, idempotency, order, concurrency and frozen manifests are enforced. Fixture mode requires both explicit development configuration and its flag, refuses Immich credentials, and has no production fallback login. Global new-session issuance and request/stream limits are bounded; expiry maintenance removes only expired sessions/unused previews and preserves saves/referenced previews. Diagnostics omit raw error messages/stacks/request data.

**Merge verification:** all 45 tests passed in fresh PostgreSQL 16 CI, including all3 real database integration cases; typecheck, lint, strict docs/media links and Docker container build passed at exact 639e0a9. The runtime image copies production dependencies only and omits client source maps. Browser run at 04:43:40UTC passed the full keyboard and 844×390 touch routes, refresh/resume, growth/jump/finish, simultaneous touch inputs, pointer-cancel/blur, audio-preference persistence, portrait 390×844 layout and scene disposal, zero page errors. Playwright 1.63.0 / Chromium 153.0.8010.12 with software rendering; no physical Safari claim. Browser harness storage was in-memory, so actual application-restart persistence remains the deploy gate.

**Adversarial review:** Fable 5.1 xhigh exact ad4eedc findings are in [WO-009 results](work-orders/009-fable-review-results.md), with [lead resolutions](work-orders/009-fable-review.md). Actual session model verified; records integrated c760e31/7795daf, review-delivery PR #22 closed as incorporated, separate CLI session reaped. Movement, packaging, coverage, error mapping, date-revalidation and record-maintenance findings resolved in 322e2f6/e5bb6d4. Safe synthetic scratch evidence retained at root `test-results/fable-review/`.

## Data and hosting boundary

Prepared app/Immich/db-init ExternalSecrets merged in haynes-ops[#2848](https://github.com/thaynes43/haynes-ops/pull/2848), all Synced. Dedicated role/database provisioning[#2849](https://github.com/thaynes43/haynes-ops/pull/2849) `705d49b` and exact-DNS ndots fix[#2850](https://github.com/thaynes43/haynes-ops/pull/2850) `fb7bc56` merged and Flux/Job passed. App-only login verified database ownership and no superuser/create-role/create-database privileges. Administrator credentials are provisioning-only. The fixture runtime mounts **only** `haynes-quest-secret`; never `haynes-quest-immich-secret` or `haynes-quest-db-init-secret`.

Internal Traefik's LoadBalancer is 192.168.40.203. UniFi manages haynesops.com, which public Cloudflare external-DNS excludes. Deployment policy permits only internal-Traefik ingress and DNS/Postgres egress. No public Pages or new public ingress is authorized.

The private Immich adapter implements fail-closed person resolution, opaque choices/IDs, per-person filtered bounded discovery, finite selection and revalidated/sanitized media. It scans at most 8 × 100 images, samples 96 candidates across that scan, then freezes up to 24 selected; truncated coverage is explicit. Sharp decodes/re-encodes thumbnails and strips metadata under pixel/output/time bounds. Dates/ages stay frozen after source date edits; permission revocation still blocks media. UTC date extraction is provisional until real-person activation settles source-local date/timezone/age anchors. The private factory exists but fixture routes cannot use it; OAuth/admission wiring is deferred.

Isolated temporary Jobs verified Immich 3.1.0 people/search schema and one bounded preview download: JPEG, 289,945 bytes, valid signature. No names, IDs, dates, bodies or media were logged/retained; Jobs deleted. This proves schema/transport only, not an admitted-player application flow. No family reference entered an external generator or static site.

## Candidate inventory and resources

The [catalog](../docs/assets/catalog.md) has real versioned pages for every scoped ID. Eight original concept images were generated serially, inspected and retained with prompts and provenance. Four sound cues have source/processed WAVs, measurements and playable auditions; one silent UI take was rejected and rerun. Audio generation queue is released. This session cannot receive audio playback, so listening review is explicitly pending.

Both v001 traveler models are selected by the lead for Tom: 11,296 triangles / 8 materials / 16 joints each; infant 0.75 m / 448,656 bytes, child 1.20 m / 460,616 bytes. Both have idle/move/interact; child adds jump. Masters, exact GLBs, matching stills, age comparison, clip videos, scripts and manifests delivered under [WO-007 evidence](work-orders/007-blender-travelers-evidence.md). Khronos has 0 errors/warnings; Three.js load/skinning and actual Chromium clip/touch/viewer/download checks passed. Lead inspected matching source/render/browser/motion views and recorded simplified cloth/hood/motion limits. No gameplay approval. WO-007 released the scene at 04:37:50UTC; masters remain `/workspace/haynes-quest/travelers/v001`.

Current WO-010 produces memory-keepsake, three clearing-path-kit pieces, clearing-tree, clearing-stone and arrival-landmark. Creator uses compact turntable contact sheets plus exact-GLB renders and the interactive viewer; lead accepted that bounded static-model review format. Root owns existing `concept-provenance.json`/review pages; the author owns final model manifests, exported media and scripts.

Blender endpoint `http://blender-authoring.dev.svc.cluster.local:8000/mcp`; artifact transfer `/artifacts/<relative-path>` reads its separate durable PVC. **Never factory-reset the live scene**: it unloads the addon. The traveler author recovered only the dedicated Blender pod after that mistake, declared/ended scoped activity, and saved the corrected scripts; dev-env was untouched. Audio service is separate and persisted. [Completed pre-MVP activation evidence](../docs/ops/003-authoring-activation-evidence.md) records the one earlier authorized dev-env roll; do not repeat it.

## Remaining owner/product work after tonight

Authentik-only OAuth client/callback and admitted-player policy; real authorized subject/media flow and useful ambiguity previews; source-local date/age-anchor choices; exact-version visual/audio review; physical iPad/iPhone Safari play/performance; private person-specific likeness design. Broader combat, era enemies, story, extra biomes, music/voices, gamepad and automatic photo-to-3D remain backlog. Do not mark owner questions answered by provisional defaults.
