# WO084: Familiar controls publication and hosted acceptance

Status: Paused at Tom’s request, September 13, 2026, until next week’s usage reset. Application PR44 is merged and its image is published. Tom subsequently requested deployment before pausing. Operations PR2898 merged as 96c09bc0ba6c800858025593c606ae19f1edac05 after nine green checks and root render review. Exact Flux rollout and all 24 isolation checks pass; full hosted gameplay, sound and touch verification passed. Further work is paused for the owner’s next-week feedback. See HANDOFF for the exact resume sequence and unresolved API verifier review. This record supplements [WO082](082-familiar-controls.md) and [the release evidence index](../evidence/plan011-hosted-release.json).

Application PR44 merged as `191f8c2420aa00e98caf026af334987aee98e593` after all checks at `a17ca8b`. All 551 tests, including 12 real PostgreSQL cases, pass alongside strict documentation and the container build. Runtime/package/lockfile/build configuration content matches the accepted local `2fbf4a2`; subsequent changes were evidence and test helpers.

## Final local acceptance and preserved failures

The final `plan011-retry-live-boss-fixed-4` completes the Besties chapter with ordinary-4 alive at HP7. The boss engages and displays health; ordinary locator touch presses produce Attack 11→8 and Bash 8→5, followed by defeat, major collection and completed journey. It traverses the 21-platform route, safe practice catch/retry, local fall, ferry and side path. The intentional HP defeat starts 0.67m from the ordinary enemy. Both stalled retry requests time out with the same payload identity, then the manual retry restores the same journey at party-grove-safe, full health, preserved progress and local recovery counter 1→0. Unexpected HTTP, page and console errors are empty.

An earlier real browser run exposed `503 SAVE_DATA_INVALID`: persisted-state validation still required all ordinary enemies defeated at three boundaries. The scoped correction and new Hono/store regressions cover both chapter transitions and preserve required boss victory, both minor memories and archived route rules. The full earlier failure remains on the PVC.

Later retained test failures are distinguished from that production defect. One locator tap returned false after its helper swallowed the exception; its exact cause is not established. The revised diagnostic retains DOM/status and actual locator errors, with a bounded physical-touch option; the final passing run needed no fallback. Another run injected the two network stalls at an incidental earlier defeat; fault injection now arms only at the intended memory checkpoint case. A third approached only within ranged wand targeting distance and could not provoke the ordinary enemy; the deliberate-death setup now measures actual strike distance. No gameplay state was injected, and no game timing or difficulty was changed for these corrections.

The versioned v006 guide retains all four inspected phone/tablet controls layouts, actual safe practice and automatic checkpoint captures, plus the separate real held-stick/Jump, release/cancel, same-orientation resize and rotation checks. Those identify their actual earlier source and are not relabeled as later server evidence.

## Paused follow-up

The exact private release is live and verified. The owner requested this deployment before pausing for next week’s usage reset and playtest feedback. Checkpoint PR45 carries the final evidence and resume instructions; its documentation merge/formal PLAN011 closeout is intentionally deferred with this handoff. PLAN010 personal identity/photos remains required next. The separate hosted API verifier has three documented false-pass gaps and was not used for acceptance; fix and independently test that optional verification utility before future use.

The owned local fixture PID1822126 is stopped and the browser lease is released. The scoped release activity is recorded in HANDOFF; dev-env was not restarted. The two Sol engineering lanes hit the usage limit; do not assume their final pending tasks completed. Registry evidence and image-only candidate validation are preserved under `/home/dev/work/quest-controls-release/.private/test-results/plan011-controls-release/evidence/`. Root reviewed the operations render comments before merging, but has not accepted the hosted API verifier; its three independent-review gaps are listed in HANDOFF.


## Hosted acceptance after the owner resumed deployment

Operations PR2898 merged at 04:28:14 UTC after final root review and a fresh baseline. Flux applied the exact revision and all 24 isolation checks passed. The full hosted normal-control run `plan011-hosted-final` then passed on the exact client, 04:29:25–04:35:58 UTC: both v2 courses, all ten fights/six memories, safe practice, branches/ferries, four responsive layouts in each chapter and automatic memory recovery in both chapters. Garden recovered to `(0,0.3,-72.8)` with revision16→17; Besties to `(0,0,-81.3)` with revision55→56. Both restored HP10 and preserved progress. Unexpected response/page/console errors are empty. The separate hosted sound-button/failure/retry check passes. Final input/resource cleanup is recorded in HANDOFF. All six hosted guide PNGs and the client exactly match accepted local bytes; the guide and catalog are served. A new full model-viewer audit is not claimed.


The hosted input diagnostic also passes with `QUEST_E2E_ROUTE_ONLY=true QUEST_E2E_INPUT_ONLY=true`, label `plan011-hosted-input-verified`, on the exact client. An earlier invocation omitted ROUTE_ONLY and incorrectly entered the legacy traversal helper; preserve `plan011-hosted-input` as an invocation failure, not a game failure or passing input check. The full authored route already independently passed. The sound, route and corrected input error arrays are empty. Browser lease is released, owned fixture remains stopped, and scoped activity act-042810-1845665 is ended. No further sessions were launched after the native Sol usage failures.
