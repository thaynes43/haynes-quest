# WO084: Familiar controls publication and hosted acceptance

Status: Paused at Tom’s request, September 13, 2026, until next week’s usage reset. Application PR44 is merged and its image is published. Operations PR2898 has nine green checks but is deliberately held as a draft; deployment, hosted verification and closeout remain pending. See HANDOFF for the exact resume sequence and unresolved API verifier review. This record supplements [WO082](082-familiar-controls.md) and [the release evidence index](../evidence/plan011-hosted-release.json).

Application PR44 merged as `191f8c2420aa00e98caf026af334987aee98e593` after all checks at `a17ca8b`. All 551 tests, including 12 real PostgreSQL cases, pass alongside strict documentation and the container build. Runtime/package/lockfile/build configuration content matches the accepted local `2fbf4a2`; subsequent changes were evidence and test helpers.

## Final local acceptance and preserved failures

The final `plan011-retry-live-boss-fixed-4` completes the Besties chapter with ordinary-4 alive at HP7. The boss engages and displays health; ordinary locator touch presses produce Attack 11→8 and Bash 8→5, followed by defeat, major collection and completed journey. It traverses the 21-platform route, safe practice catch/retry, local fall, ferry and side path. The intentional HP defeat starts 0.67m from the ordinary enemy. Both stalled retry requests time out with the same payload identity, then the manual retry restores the same journey at party-grove-safe, full health, preserved progress and local recovery counter 1→0. Unexpected HTTP, page and console errors are empty.

An earlier real browser run exposed `503 SAVE_DATA_INVALID`: persisted-state validation still required all ordinary enemies defeated at three boundaries. The scoped correction and new Hono/store regressions cover both chapter transitions and preserve required boss victory, both minor memories and archived route rules. The full earlier failure remains on the PVC.

Later retained test failures are distinguished from that production defect. One locator tap returned false after its helper swallowed the exception; its exact cause is not established. The revised diagnostic retains DOM/status and actual locator errors, with a bounded physical-touch option; the final passing run needed no fallback. Another run injected the two network stalls at an incidental earlier defeat; fault injection now arms only at the intended memory checkpoint case. A third approached only within ranged wand targeting distance and could not provoke the ordinary enemy; the deliberate-death setup now measures actual strike distance. No gameplay state was injected, and no game timing or difficulty was changed for these corrections.

The versioned v006 guide retains all four inspected phone/tablet controls layouts, actual safe practice and automatic checkpoint captures, plus the separate real held-stick/Jump, release/cancel, same-orientation resize and rotation checks. Those identify their actual earlier source and are not relabeled as later server evidence.

## Release work remaining

The operations lane owns `/home/dev/work/quest-controls-release` and its private PLAN011 tools/evidence. Root will review the exact image-only change, require all nine checks/render comments, declare the scoped rollout, and verify the exact image plus protected normal Quest/dev-env state. The browser lane then receives the hosted lease for the full two-chapter route and relevant controls, sound and catalog checks. Close PLAN011 only after these pass; PLAN010 remains the next required personal-photo stage.


The owned local fixture PID1822126 is stopped and the browser lease is released. No release activity was started and dev-env was not restarted. The two Sol engineering lanes hit the usage limit; do not assume their final pending tasks completed. Registry evidence and image-only candidate validation are preserved under `/home/dev/work/quest-controls-release/.private/test-results/plan011-controls-release/evidence/`. Root has not yet reviewed the operations render comments or accepted the hosted API verifier; its three independent-review gaps are listed in HANDOFF.
