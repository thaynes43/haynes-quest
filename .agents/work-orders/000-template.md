# Work order: replace with bounded task name

- **Status:** Draft / In progress / Ready for review / Complete
- **Model / dispatch:** Select explicitly under [TEAM.md](../TEAM.md): native Sol `xhigh` by default; **native Astra `max` for all Blender work**; separate Fable 5.1 `xhigh` via `agent-run` for authorized coding/adversarial review
- **Context:** Fresh development agent; native `fork_turns: "none"`; self-contained brief and repository inputs
- **Required reading:** Applicable instructions, handoff and named design/plan sections
- **Worktree / branch / base commit:** To fill; owned paths must be explicit
- **Depends on / stable contracts:** To fill
- **Owned paths and remote resources:** To fill; include exclusive Blender scene owner and release condition if relevant

## Outcome and scope

Describe the authorized result, current decisions and boundaries without referring to the planning conversation. Supply enough context for a new agent. A Fable code/review session may delegate to Opus under Claude policy but must return Blender tasks to the Astra lead.

## Inputs and authoring contract

Name the stable memory/ability/asset/cue IDs and exact input versions. For modeling include the lead's selected concept files, art-brief version, style references, baseline scale/orientation, clip names, provisional budgets and comparison camera angles. Image generation stays with the driving Astra; the Blender agent does not create a competing visual direction.

Supply verified MCP endpoint, remote scene/workspace ownership, actual upload/download method, durable output directory and input checksums. Local worktree paths are not visible to the remote service automatically. State when exclusive ownership starts/ends; different filenames do not isolate a shared running Blender scene.

## Deliverables and verification

Return runnable code or editable masters/build scripts/GLBs and previews, exact checksums, actual commands/results and known limits. Model handoffs include matching concept/render views and animation examples; audio handoffs include original/processed audition files and processing/level evidence. Link the versioned catalog record; do not claim owner approval, listening or device tests that did not happen.

For adversarial review identify the exact commit and request concrete counterexamples, severity and reproduction evidence. Reviewers do not edit unrelated scopes or merge unreviewed fixes behind the coordinator.

## Handoff and recovery

Record branch/commit, artifact paths, changed contracts, scene release, active jobs, unresolved findings and next action. The coordinator intakes and iterates candidates; only Tom-approved exact versions enter final gameplay. On usage/tool failure save the partial work and actual error, then return ownership cleanly. Do not downgrade Blender to a different model.
