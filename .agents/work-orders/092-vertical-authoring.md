# WO092: Optional platforms and reusable vertical sections

- **Status:** Completed, September 20, 2026
- **Dispatch:** Native Sol Ultra for validator/runtime corrections, controller/browser verification and release; authorized Opus 5 xhigh for bounded shared-command/CLI implementation and review; Astra for architecture, UX, text and final review.
- **Plan:** [PLAN013](../plans/completed/013-vertical-level-authoring.md)
- **Source:** [App PR54](https://github.com/thaynes43/haynes-quest/pull/54), `c7f034af16ff39f0007d76697569db382a83d562`; its tree exactly matches reviewed candidate `cf038e9d55b77280cafc8057338cd7fdd022e11f`.

## Delivered

Version 2 permits optional floating and moving platforms outside named routes. Version 1 behavior, required objectives and explicit connection checks remain intact. The visual Add panel and agent CLI share atomic `section.add`: raised arches or zigzag ridges, 2–12 climbing steps, broad editable platforms, directed connections, platform checkpoints and a side branch. Existing main routes and anchors remain unchanged.

Spatial CLI inspection exposes heights, bounds, route/support relationships, safe-landing backlinks and shared limits. The guide includes defaults and a 12-step welcome-to-woodland-rest left arch at 0.3 rise, with a 3.6-unit crest. Command rejection preserves the draft and its actual validation status; undo/redo includes the whole section. Surface review findings on integer inputs, selection, fitting defaults, naming, feedback and mobile layout were fixed.

Planner review corrected uneven long-span descent and local obstacle avoidance, with early scene-budget checks. Controller tests prove infant/child traversal, original routes and fall recovery. A winding-east centre-seeking probe hit its existing sweeper; both side lanes pass at five phases for both avatar sizes and patterns. It remains a normal obstacle, not a generator failure.

## Verification and release

757 CI tests pass, including 12 real PostgreSQL cases; typecheck, lint, four immutable course documents, production/container builds and strict docs/media checks pass. Local Chromium passes the dedicated vertical flow, all 18 existing editor scenarios, three authoring groups and the exact tall-example geometry preview. Hosted Chromium repeats optional platform save/reload/preview, section undo/redo, all eight generated edges using normal input, deliberate crest recovery and phone/tablet control dispatch. All final page/console/HTTP error arrays are empty. Physical Safari and hardware performance are not established by emulation.

[Ops PR2992](https://github.com/thaynes43/haynes-ops/pull/2992), merge `275b0f8d087a87cd3d11b12e019c21bce6470071`, changed only the private image pin and source comments. Publication, Flux/Helm generation 13, exact hosted JS/CSS, editor/guide/health endpoints and unchanged normal Quest/dev-env are verified. Activity `act-045405-291465` ended. No asset production, family-photo access, normal-Quest deployment or dev-env restart occurred.

## Durable evidence and cleanup

The [release index](../evidence/vertical-authoring-release.json) records image/source, exact hashes, CI, report hashes and artifact paths. Raw reports, screenshots, reviews, CLI example and release evidence are archived at `/home/dev/artifacts/haynes-quest/vertical-authoring-20260920-c7f034a`. `tall-section-editor.png` is the shareable image and `tall-garden-project.json` is a portable example. The browser summary distinguishes discarded camera-probe artifacts from acceptance evidence.

Local fixture `quest-vertical-candidate` is stopped; port4430 is closed on IPv4/IPv6 and browser contexts are closed. Separate CLI task worktrees were reaped after intake and evidence preservation. Repository-only closeout records do not require another game image promotion. No Blender scene or audio job is owned.
