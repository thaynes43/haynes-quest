# WO081: Playground publication and hosted acceptance

Status: Completed, September 12, 2026. Application PR41 and private operations PR2872 are merged. The exact client is live; full hosted route, API, input, audio, landscape and catalog checks pass. PLAN009 is closed for daughter-focused creative inspection. The [sanitized evidence index](../evidence/plan009-hosted-release.json) retains exact report hashes and actual scope.

## Published and deployed version

Application [PR41](https://github.com/thaynes43/haynes-quest/pull/41) merged as `ee468101700ed3d0a5f94ffde4226a740a3d3ac4`. All PR checks passed at `dd14285`. Main Application `34715695671` and Documentation `34715695670` passed, including 452 tests (10 real PostgreSQL), production build, strict documentation, container publication, provenance/SBOM, attestation and signing. Anonymous registry headers and independently hashed manifest bytes match the published digest; independent signature verification is not claimed.

Image: `ghcr.io/thaynes43/haynes-quest:sha-ee468101700ed3d0a5f94ffde4226a740a3d3ac4@sha256:9862c6c8f95c74b6c0c5f944125ae7aefe4729f211340a3f43d417e1f663459d`.

Operations [PR2872](https://github.com/thaynes43/haynes-ops/pull/2872) merged as `bdbfc04afe24f8e9046459a62779698e6f0bb8b5` after all nine required checks and independent exact-head review at `8e5f5b7`. Rendered main diffs contain only the private image replacement; edge diffs are empty. Flux applied the exact merge revision. All 24 post-release assertions pass against the immediate pre-merge baseline: private readiness/image, normal Quest and dev-env identities/restarts, services, routes, policies, health and source reconciliation. The same 24 assertions pass again after the full hosted run in `post-hosted-release.json`. Scoped activity `act-201013-1664542` is ended.

Client: `index-DOmWuEtB.js`, 1,171,500 bytes, SHA256 `40c5715544d0993a2e782065f06be82c29c1b0124dacf6a0ab49136b0f1f29ea`. Merged runtime/package/lockfile/Vite/Dockerfile content matches accepted local source `cfaddf4`. Hosted API probe passes all 59 checks, including exact client bytes, catalog v4, both authored routes, five unique encounter slots per chapter, fresh reset and six exact fictional pictures.

## Browser acceptance

[WO078](078-plan009-playgrounds.md) and the [v005 guide evidence](../../docs/assets/media/playtest/v005/playground-check.json) record local acceptance: full two-chapter control journey, nearby recovery, both attacks and bosses, six memories, branches/ferries/hazards, held-stick/world-tap input, mobile sound failure/retry, authored landscape and the visual catalog. The local full journey took 359.742 seconds, including intentional checks and real combat retries. It uses keyboard movement/jumps with touch combat; separate actual-stick/world-tap checks cover mobile input. These are not child pacing or physical-device performance evidence.

Separate hosted held-stick/world-tap input, touch smoke, scrolled audio failure/retry/repeat, and the visual catalog pass on the exact live bundle. The catalog reports 37 entries, 54 navigation thumbnails and 26 model deliveries/viewers with no unexpected errors. Native Sol completed the full final route and released the browser lease. The full run checks Garden landscape; a separate limited Besties check supplies its landscape coverage. One explicit artwork retry while fallen succeeds. No gameplay state injection is permitted.

Two earlier hosted failures are preserved. `plan009-final-hosted-route` completed garden, then a fixed two-sample ferry check failed at Besties initialization; there is insufficient evidence to assign an exact phase cause. The revised helper observes every declared piece across live samples for at most six seconds, keeps the original movement threshold and fails frozen/missing pieces. It records physics times, sample count and maximum observed movement. Direct delayed-motion/frozen/missing probes and independent native review pass.

`plan009-final-hosted-route-observed` proved dynamic motion in both chapters, actual ferry carry and the first three Besties fights, then exposed cumulative attempt bookkeeping: an edge already landed on used its old attempt count when revisited after later hazard recovery. Once exhausted, an undefined observation was mistaken for a document exit and caused a TypeError. The narrow driver repair resets the budget only after a proven landing/permitted finish and explicitly rejects missing observations. Three unresolved attempts and ten total recoveries remain the limits. Two regressions fail on the old driver and pass on the repair, including the original TypeError. No gameplay, input timing or difficulty changed. The full suite reports 444 local passes with 11 dedicated PostgreSQL cases skipped; TypeScript and focused lint pass. The final full hosted run is `plan009-final-hosted-route-verified` and passes in 268.494 seconds. It visits both selected side paths, completes all ten fights/six memories/six friendly contacts, rides both ferries with zero offset drift, jumps six live hazards and proves exactly two intentional checkpoint recoveries. Each boss takes one real combat retry. Both Besties move and react through their clips, then hide after defeat. One expected model failure is restored with one Retry while fallen; physics stays paused. All unexpected error arrays are empty.

A further coverage omission was found in the test helper: carried equipment bypassed the later chapter's pickup and landscape branch. The first controls-only diagnostic then ran a full chapter and returned raw success with an empty layout; retain `plan009-final-hosted-chapter2-landscape` as a coverage failure, not a passing landscape check. Pickup checks now use the active chapter and landscape runs at the guard platform independently of prior contact collection. A new guard rejects controls-only runs that reach whole-chapter completion without the layout check. The corrected limited `plan009-final-hosted-chapter2-landscape-verified` passes in 28.833 seconds: four opening terrace crossings, no recovery, the guard platform, 844×390 controls/overlap bounds and Help open/close. Full route evidence uses the `e15488e` helpers; this limited check uses `6a136f8`; the later negative coverage guard is `d5de1f4`. The index distinguishes these exact test versions. No further full-run or independent-suite repeats were needed for this coverage repair.

## Durable evidence and next stage

Root release worktree: `/home/dev/work/quest-playgrounds-release`, reports under `.private/test-results/plan009-release/`: `publication.json`, `render-candidate.json`, `pr2872-review.json`, `pr2872-independent-audit.json`, `baseline-immediate.json`, `ops-merge.json` and `post-release.json`. Root application/evidence worktree: `/home/dev/work/quest-daughter-levels`; API report `test-results/plan009/review-evidence/playground-api-hosted.json`. Closing worktree: `/home/dev/work/quest-playgrounds-closeout`. Browser reports use distinct `plan009-final-hosted-*` labels; all exact paths/checksums are in the evidence index. Root visually inspected the hosted woodland and restored Besties captures. Owned fixture4400 is stopped; unrelated4392 is untouched. No shared browser or scene lease remains.

No new art or audio was generated. The same 23 runtime GLBs, four source WAVs, six fictional pictures, 37 catalog entries, 54 thumbnails and 26 viewers remain. Guide v005 captures/check records describe the longer courses; exact final asset review remains separate. No dev-env restart or private-photo/credential access occurred.

The scoped release is complete; current records and the completed plan are carried through the checked closeout PR. Daughter playtesting, broader physical-device performance and exact final asset approval remain owner inspection. Identity and curated personal photos remain the next required MVP stage under [WO080](080-identity-curation-readiness.md); the fictional fixture is not a personalized MVP.
