# WO079: Authored playground adversarial review

Status: Ready for separate review. Reviewer: exact Claude Opus 5 (`claude-opus-5`, `xhigh`) through `agent-run`, per Tom's temporary Fable quota instruction. Lead owns integration and user-facing text.

Review exact candidate `f2bfe36` on branch `agent/quest-daughter-levels` in the local `haynes-quest` repository. Your task worktree starts from main; run `git merge --ff-only f2bfe36` inside that worktree to review the candidate without changing the lead's worktree. Read AGENTS.md, TEAM.md, PLAN009, DESIGN017 and WO078. Do not restart dev-env, query private media, start asset generation, deploy or merge a PR. The root's native browser agent owns the browser lease and port 4400; do not attach to that browser or modify the fixture.

Find concrete regressions in the new authored document, validation and runtime adapter, version compatibility, encounter binding, checkpoint/retry behavior and translated Besties combat. Check whether validation accepts impossible or unsafe required placements and whether archived routes/plans remain unchanged. Examine tests and add focused adversarial probes in your own worktree if useful. This is an engineering review; the lead owns UX/copy and still must inspect new captures and finish the browser journeys. The current guide has not yet been updated for the new layouts; that known pending task is not a new finding.

Return a concise review with file/line references, severity, reproducible evidence, commands and actual outcomes, including a clean-review statement only if warranted. Save it under `.agents/work-orders/079-plan009-adversarial-results.md` in your worktree and commit your review/probes on your task branch. Do not modify production code or the lead's files. Tell the lead the commit, worktree, report path and unresolved findings in the final session output. Use native Opus subagents only if a bounded independent task warrants them; no Fable or Blender dispatch.
