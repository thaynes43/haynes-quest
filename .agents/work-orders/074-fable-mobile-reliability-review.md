# WO074: Fable adversarial review of mobile reliability

Status: dispatched by Astra, September 12, 2026. Reviewer: separate Claude Fable5.1 (`claude-fable-5-1`, xhigh) via agent-run. Review exact application candidate `717cfe6` from local branch `agent/quest-mobile-reliability` in your own agent-run worktree. Read AGENTS.md, TEAM, PLAN008 and DESIGN014/015/016. Do not modify the root worktree `/home/dev/work/quest-mobile-reliability` or canonical clone.

The physical iPhone user reported: no audible sound and an apparently inert Help test button; collected minor keepsakes stay rendered; Attack beside one seems to open a popup; Besties stay standing and fail to meaningfully threaten the player. The previous green source/mocked-renderer reviews missed those failures. Scope this review to concrete adversarial regression findings, not a rubber stamp of the old suite.

Candidate changes: v3 revealed keepsakes hide (archived v2 preserved), accepted contact prevents an extra same-frame attack notice, permanent jump hint removed, scrolled Help has an explicit repeatable sound status, context resume/load has bounded recovery after interruption, aimed v3 boss warnings freeze their geometry for dodging, shared actor anchors drive targeting, and defeat is processed even when victory disables combat. Black's center lane can now hit a player staying on the middle path. No extra model/audio generation, OAuth, private photos or dev-env restart.

Own only `.agents/work-orders/074-fable-mobile-reliability-results.md` and `tests/review/074-probes/` in your worktree. Fast-forward your worktree to 717cfe6 (local branch/commit accessible), install locked repo dependencies if absent, then inspect and challenge the changed paths. Use actual supplied GLBs for at least one runtime AnimationMixer probe: prior tests only proved clip inventory and synthetic clips. Look for render state/positions diverging from simulation, contact input swallowing or replay, defeat while paused, touch target overlap, gesture races, hanging audio promises and stale async UI results. Return source references, actual failing probes, severity and material limits; distinguish a demonstrated bug from a hypothetical Safari problem. Do not directly patch runtime or write user-facing docs/copy; Astra integrates fixes.

Do not use shared Playwright MCP (no lease) or touch fixture4392. You may use your own local Chromium process against root's owned fixture http://127.0.0.1:4398 for read-only/ephemeral test interactions; report its actual bundle, as source may advance during review. Create no persisted/private data. Root is already running the full browser route, so prefer source/actual-GLB probes to avoid two heavy SwiftShader routes at once. Cached WebKit cannot run due missing host libs; do not install dependencies or alter MCP config.

Commit and push your results/probes to your review branch; do not open or merge a separate PR. Write final handoff with reviewed commit, checks, findings and output paths. Then stop. Lead carries the integrated checked PR/release.
