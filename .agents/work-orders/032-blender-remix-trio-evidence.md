# WO032 authoring evidence

- Fresh native Astra max, owner `/root/remix_trio_authoring`, worktree `/home/dev/work/quest-remix-trio`, branch `agent/quest-remix-trio`, base `d167b41`.
- Current stage: live WO032 lease granted by root after WO028's release. Claimed at `2026-09-11T17:09:10.483651+00:00`; Sir Flush-a-Lot final media is rendering. Nap Captain and Diva remain prepared, unbuilt sources.
- Read AGENTS/TEAM, DESIGN002/005/007/011, prior WO027 delivery and tools; viewed all three exact concept images and read their prompts, provenance and root review corrections.
- Scope is Sir Flush-a-Lot, Nap Captain and The One-Star Diva v001 only. Root concepts and user-facing review records remain root-owned.
- Fixed contract planned: heights 1.00/1.05/1.65 m including crown/cap/bow. Attack duration/contact/fraction: Flush 1.6/1.0/0.625; Nap 1.5/0.9/0.6; Diva 2.0/1.25/0.625. Final exported measurements must establish these values before delivery.
- Construction rulings retained: one Flush head and a lid that clears the lowered crown; Nap pillow under character left arm in neutral, continuous tail/cord/mask strap; Diva paddle gripped by character right hand in every clip, single star front and lavender back, complete connected coat/belt/bows and visible feet.
- Local preparation does not establish an exported model, visual acceptance, motion, transfer, browser, device, owner approval or deployment result. Early actual GLB front/back/three-quarter checkpoints will go to root per model after lease grant and before final media production.

## Live checkpoint: Sir Flush-a-Lot

Verified and preserved WO028's `/workspace/haynes-quest/parody/block-party-pair/v001/live-scene-release.blend`: **13,540,486 bytes**, SHA256 `d89132f2ba7526a78b933aca4dea9de248cc7653a710d09e49abd2e5885c7b3c`. At intake only service PID1, XvfbPID9 and live BlenderPID12 were present. No factory reset or addon change was made. The missing `ps` executable was handled by reading `/proc`; this did not affect the previous scene.

The root selected the actual Flush front/back/three-quarter checkpoint, retaining its face, silhouette, crown/bow tie, cistern, stable base and rear emblem. Root subsequently selected the corrected contact/followthrough and defeat front/side views. The final current GLB is **782,488 bytes**, **14,676 triangles**, **five opaque material primitives**, **13 joints**, full height **1.00 m**. SHA256: `c4db8f231fcdb29d71ddc605df89b928ef0aeaa29b91af3b1e3041041f0cdf49`. Attack measures 1.6000000238 seconds in Three; contact remains 1.0 seconds / fraction0.625.

The exact export passes Khronos with zero errors/warnings/infos/hints, actual Three geometry/ground/cloning/binding checks, and isolated Chromium153 WebGL raster checks for all five clips. Source vertices and weights survive export; all required physical attachments stay connected through six samples of each clip. A separate 17-sample duck audit finds no lid/head/hair/crown intersections. The final media renderer is PID51826; its still/video work has not yet completed.

The first duck tests correctly caught clipping. The final defeat compresses the head to fit inside the bowl and removes pitch under the compressed neck, which otherwise multiplies the head's compensated scale into backward crown travel. Neutral geometry is unchanged. A checker coordinate conversion was also corrected to use imported armature space. Earlier exact sources/renders remain in the asset's remote `iterations/` directories. The editable master keeps its normal armature parent; `export_armature_object_remove` gives a valid root skinned node in GLB without validator warnings.

All owned process handles now live in Blender's persistent driver namespace, with a durable status snapshot in `scripts/assets/parody-remix-trio/render-process-completion.json`. The first quick-render handle was recovered from Python's active-process registry, observed exit0, explicitly waited and reaped. Failed exploratory checker exits are recorded, not described as successful final checks. The sole live lease remains held until the complete trio is delivered and all jobs are reaped.

## Offline checkpoint

All three original 1024px atlases and the complete per-character construction/five-clip scripts are prepared. `common.py` retains named construction meshes, weighted shoulders/tail/legs where needed, an explicit fixed root, floor compensation on the body joint, and the export contract. Root-owned concept/prompt/provenance files are unchanged. The source colors and grain are original procedural work, separate from the selected concepts.

The WO027 real GLB rendering, Three cloning/binding, browser raster and Khronos tools have been parameterized under this work order's own paths. `compare-construction.py` matches named source vertices and weights against reimported runtime geometry, then measures required attachment surfaces through six samples per clip. Source authoring and final video sampling use 30fps deliberately: 1.6, 1.5 and 2.0 seconds become exact whole-frame durations. The export-review importer still uses its explicit 24fps scene clock and samples the GLB at the corresponding seconds. That media change was sent to root; no media has yet been rendered.

Local Python syntax, Node syntax and whitespace checks pass. Atlas generation used locally cached Python3.13.1, NumPy2.5.2 and Pillow12.3.0; the system Python3.11 could not use those compiled cache packages and was not modified. Coarse offline triangle estimates were used to reduce hidden/small-detail density before live work; only the future actual GLB validator can establish final counts.
