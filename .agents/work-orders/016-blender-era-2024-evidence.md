# WO-016 authoring evidence

- Author: fresh native `gpt-6-astra`, `max`, `/root/era_2024_authoring`.
- Worktree: `/home/dev/work/quest-era-2024-models`, branch `agent/quest-era-2024-models`, base `abee156`.
- Status: Offline preparation complete. Waiting for the root's explicit scene lease.
- Scene: WO-014 `/root/era_2020_authoring` still owns the only live Blender scene. This author has made no Blender MCP request, launched no Blender process and read no live scene data.
- Remote directory reserved after lease: `/workspace/haynes-quest/era-2024/v001`.

## Prepared source

`scripts/assets/era-2024/build.py` constructs Loop Dancer, Prism Mimic and Trendweaver from the exact selected sheets, whose checked hashes are in `source-concepts.json`. Each sheet was visually inspected. The source uses a shared six-material palette, sculpted rounded shells and relief, a retained original procedural pigment graph baked to one embedded 1024-pixel atlas, independent part and ribbon bones, fixed root and five clips. It joins geometry before export to target six color-pass primitives.

The creature constructions retain two arms/two legs/two scarf tails and key for Loop Dancer; four feet/two ears/one curved tail and one mask face for Prism Mimic; and five crown arches/two arms/two rear ribbons/three underside bobbins with a left-hand shuttle for Trendweaver. Final shape/count claims await actual export inspection.

`render.py` is prepared for exact-GLB re-import, front/side/back/beauty images, bounded clip videos, motion grid and turntable. `validate.mjs` checks exact bytes with Khronos and the five-clip/resource contract. `inspect-three.mjs` is prepared to decode the embedded atlas and measure actual skinned vertex movement, root stability, loops and bounds through the app's pinned Three loader.

Static checks: all Python sources parse with Python 3.11 AST; both JavaScript files pass `node --check`. These checks do not establish Blender execution, export correctness, visual quality, measured budgets or final animations. No candidate outputs have been generated or approved.

## Next after lease

1. Upload source to the assigned remote directory; record the explicit grant and start.
2. Build Loop Dancer, re-import and render a beauty candidate, then send the actual output to root for early shape review.
3. Resolve review feedback and finish all three exact-version packages and measured validation.
4. Download and hash-check the masters and deliverables, checkpoint scoped commits, and explicitly release the scene.
