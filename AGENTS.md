# Contributor and agent guide

This project is a novelty 3D browser game, initially a family PoC for Tom Haynes's kids. Authors tailor levels to a person, placing memories from photo integrations or manual uploads alongside obby challenges and curated encounters. Reusable pieces and shared validation will support human and agent level building. The avatar starts mysterious at memory age zero; post-boss memories grow its age and abilities. Synthetic age variants serve the private MVP. Immich is the first integration. The private fixture MVP and asset studio run in the local cluster through `haynes-ops`; real player admission remains deferred.

Read [`.agents/HANDOFF.md`](.agents/HANDOFF.md) for the current stage, then the [project brief](docs/prds/001-project-brief.md) and the documents named by the active plan.

[PLAN-004](.agents/plans/completed/004-overnight-mvp.md) records the overnight private MVP and complete first-pass catalog. Its Immich adapter, visible age progression and private appearance contract supersede older generic-only and synthetic-adapter-only limitations. Read the handoff for completion evidence and the remaining OAuth, owner asset review and physical-device gates; do not repeat the completed authoring setup or restart dev-env.

Tom’s later gameplay correction is now [PLAN-005](.agents/plans/completed/005-era-combat-loop.md): equipment and era enemies lead to a boss; only after victory does consuming the memory bundle advance age and unlock the next period. This supersedes the coordinator’s memory-by-memory growth and combat-deferred interpretation in PLAN-004. The earlier demo’s passing tests do not establish the intended gameplay or visual quality.

Enemy direction remains **recognizable, funny pop-culture parodies**, selected by photo dates and birthday-derived progression. The six existing creature assets form the friendly cast: they offer optional healing bonuses, and deliberately harming them incurs a recoverable penalty. Keep them out of automatic enemy targeting and boss progression gates. PLAN006 and DESIGN013 govern their new gameplay integration; DESIGN005 governs the enemy roster.

Tom also requires forgiving Roblox-style obby sections alongside goofy fights, designed for his six-year-old daughter. Read DESIGN-011; the flat corridor and a single step are not acceptance of that gameplay pillar.

Tom now requires parent-prepared journeys and collaborative enemy selection; read DESIGN-012. Full-name Immich lookup, birthday, per-level curated photos, child-specific favorites/exclusions, and editable era-based enemy/boss choices belong to setup. Admins can correct relevance windows or permit explicit period overrides without rewriting history or bypassing photo/asset/progression gates. Discuss short encounter pitches with Tom before expensive modeling; do not automatically finish or expand the former fixed six-enemy batch. Operation Besties is implemented under PLAN006 using Tom’s approved joint pink/black look; WO051 records the completed two-model delivery. Exact final-art review remains open. FNAF remains a later interest for his son. Preserve unfinished authoring checkpoints.

[PLAN006](.agents/plans/completed/006-playtest-polish-and-besties.md) completes the private touch-combat, sound, foliage, friendly-character and Besties release. Read the handoff for exact live evidence and the remaining physical-device, listening and final-art review.

[PLAN007](.agents/plans/completed/007-playtest-reset-and-controls.md) completes the revised private playtest and supersedes the prior playtest controls and reward presentation after Tom's next physical-device feedback. Private tests start fresh without saved-progress UI; jumping is always available; world taps/Space jump; walking into pickups interacts; Attack and Secondary replace the four-button cluster. Two minor memories are collected along each route and one major memory after the boss advances age. Prior test passes do not establish physical-device quality.

## Working rules

[PLAN011](.agents/plans/completed/011-familiar-controls-and-checkpoints.md) completes the controls and recovery pass, including the September 19 portrait input and post-boss memory-guidance repairs. [DESIGN018](docs/designs/018-familiar-touch-and-recovery.md) supersedes world-tap-only jumping, central attack reminders, manual death recovery and dizzy-only Besties damage. Dedicated touch controls, automatic memory checkpoints, bosses that respond in range and versioned safe up/down practice are implemented. Read the handoff for exact hosted evidence and the distinction between browser emulation and physical-device acceptance. The preceding [PLAN009](.agents/plans/completed/009-daughter-playground-mvp.md) records the longer-course release. [PLAN010](.agents/plans/010-parent-prepared-memories.md) remains the required personal-photo stage. Keep the fictional fixture separate, old routes immutable and MkDocs current. Define the parent/admission/media contract before querying family photos; do not restart dev-env.

[PLAN008](.agents/plans/completed/008-mobile-reliability-and-builder-direction.md) completes the diagnosed sound/input, collected-memory rendering, boss behavior and paused-artwork retry repairs, removes the permanent jump hint, and verifies the private hosted route. Read the handoff for exact release evidence and Tom’s subsequent iPhone acceptance and remaining child/creative inspection. [DESIGN016](docs/designs/016-authored-levels.md) supersedes automatic name-to-journey setup as the central experience: humans and agents author distinct personalized levels, with freely placed memories, manual uploads and period suggestions that offer **Show more**. Broader choice never bypasses media access, asset readiness or progression rules.

Tom's September 19 follow-up reports his son's successful gameplay apart from a stuck enlarged phone view. [WO090](.agents/work-orders/090-app-wide-zoom-lock.md) supersedes [WO089](.agents/work-orders/089-phone-viewport-zoom.md): prevent browser zoom throughout the game app, including loading, start/setup, gameplay and menus. His next sequence is a few more enemy models, then requirements and ADRs for the level-building platform. This comes before implementing the previously scheduled PLAN010 personal-photo stage. Retain its admission/media requirements as platform inputs, and discuss encounter concepts before expensive modeling.

- Follow [`.agents/TEAM.md`](.agents/TEAM.md) for Tom's latest project routing and its temporary quota override: Fable is exhausted until Monday at 8 AM; use `claude-opus-5` at `xhigh` for any needed separate adversarial review until availability is verified after the reset. Astra (`gpt-6-astra`, `max`) coordinates and retains full lead context. Native Sol (`gpt-5.6-sol`, `xhigh`) handles ordinary bounded tasks with `fork_turns: "none"`. **All Blender work uses fresh native Astra subagents at `max`.** Driving Astra alone generates concept images, one at a time, and owns art direction, user-facing text, integration and final review. Outside the temporary override, Tom authorizes separate Fable 5.1 (`claude-fable-5-1`, `xhigh`) sessions through `agent-run` for adversarial reviews and coding to balance plan use. Claude Code uses Opus for its own subagents. These project exceptions take precedence over generic pod defaults. Give every task a self-contained work order and owned paths/resources; keep checkpoints durable through context or quota limits.
- Work in a task worktree on `agent/<slug>`. Canonical clones are fetch-only. Use branches and PRs, then squash-merge once the required checks pass. Never push directly to `main`.
- Document the requirements and significant decisions before implementing them. Follow [docs/PROCESS.md](docs/PROCESS.md), and update the relevant docs in the same PR as the behavior.
- **Keep MkDocs synchronized with every asset change.** Generating, importing or revising an asset is incomplete until the same PR updates its versioned review page, `scripts/assets/catalog-inventory.json`, and the thumbnail navigation in `docs/assets/catalog.md`, including derived thumbnails and factual status/counts where affected. This includes concept-only, partial, paused and rejected candidates, audio, and changes to approval or gameplay use. Show the correct inspiration image and available model/render or audio preview; preserve earlier versions. Follow the mandatory [catalog maintenance procedure](docs/PROCESS.md#asset-catalog-must-stay-current). Authoring agents supply the catalog handoff; the coordinator owns integration, checks and publication. Do not defer this to a later cleanup task or declare asset delivery complete while its catalog is stale. Keep private media outside public git and static MkDocs input.
- Keep the current scope explicit. The brief establishes Roblox-style direction, touch and keyboard/mouse input, Authentik-only login, saved games, configured journey subjects, a shared avatar, chronological photo memories, and an authored catalog of enemies/bosses tied to the represented cultural eras. Never hard-code personal names or require a per-person avatar asset. Photo coverage is not evidence of birth or current age; follow DESIGN-004. DESIGN-006 makes post-boss memory-bundle consumption unlock age-appropriate abilities that carry forward; use an explicit age source and ensure required paths are reachable with the current ability set. Use explicitly supplied gender/settings, not image inference. DESIGN-005 defines date-based enemy eligibility and original-art provenance; dynamic selection uses prepared catalog entries. PLAN-003 dependency setup is complete. PLAN-005 corrects the PLAN-004 prototype with required equipment, era encounters, bosses and post-victory growth in the bounded loop; the wider campaign and additional content remain deferred. Define the slice's contracts before its code; after PLAN-003, do not make the remaining full-game requirements or final asset production/review block synthetic greybox work. ADR-002 remains a proposal to validate.
- Sequential image generation by the Astra lead followed by Astra Blender MCP subagents is the agreed visual-authoring direction. Use the versioned storybook art brief and MkDocs Material asset studio under `docs/assets/`. Produce the whole scoped first pass autonomously, including source concepts, 3D previews and audio audition files, while keeping coordinator selection separate from Tom approval. PLAN-003 places Blender and its bridge in a dedicated cluster workload with remote MCP and explicit artifact transfer, upgradeable independently of dev-env. DESIGN-008 selects a separate self-hosted audio service using the official optimized Stable Audio Small-SFX CPU model and prepared browser cues; GPU render Jobs remain a later option. PLAN-003 records the completed pre-MVP tool activation. Both authoring services now work independently; no further dev-env restart is part of the MVP. Document actual readiness honestly. Tom reviews exact final visual/audio versions before they enter gameplay. Prepare concrete previews under DESIGN-007 and the asset-review template; use placeholders or prior approved versions while candidates await review. This asset gate does not require owner approval for ordinary code/docs PRs. Automatic per-person character generation is [conditional future backlog BL-01](docs/BACKLOG.md#bl-01-automatic-playable-character-generation); its workers, mock jobs, provider selection, and trials must not gate the PoC.
- Carry authorized work through implementation, meaningful validation, PR/merge, and any deployment the task includes. A documentation-only task does not imply deploying a placeholder application.
- Cluster configuration belongs in `haynes-ops` and deploys through Flux. Follow that repo's applicable instructions for any deployment work.
- Deliver a normal homelab-hosted web app for iPad, iPhone, and PC. Use Safari as the touch validation baseline. Do not introduce native iOS packaging, TestFlight, or a required app-install workaround.
- Keep Immich credentials and family photos out of git, published build artifacts, and diagnostic output. Use clearly synthetic fixtures for development until the photo-selection contract is defined.
- Give the game its own name, visual identity, and interaction design. Reuse proven engineering conventions from sibling repos where they fit.
- Keep the handoff concise and current. Completed plans retain their IDs when moved to `.agents/plans/completed/`.

## Verification

Build the MkDocs Material site and check documentation/media links using the commands in [docs/README.md](docs/README.md). Run `pnpm typecheck`, `pnpm lint`, `pnpm test` and `pnpm build`; use a dedicated `QUEST_TEST_DATABASE_URL` for real Postgres tests. The [private-preview runbook](docs/ops/002-private-preview.md) documents browser journeys and the strict docs build. CI runs the same checks. For gameplay work, document the actual device/browser journeys and frame-time targets the later brief requires.

Do not claim a release is deployed or an integration works until the relevant live checks have run.
